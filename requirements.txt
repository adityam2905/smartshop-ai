# SmartShop dependencies:  pip install -r requirements.txt
#
# Plain `stable-baselines3`, not `stable-baselines3[extra]`: [extra] adds
# opencv, pygame and Atari packages this project never uses, roughly doubling
# install time (which matters on Streamlit Cloud's free tier). tensorboard is
# listed because training logs to it.
#
# Tip for slow installs: the CPU-only torch wheel is ~200MB instead of ~2GB —
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# then install this file as usual.
stable-baselines3>=2.3.0
tensorboard>=2.16.0
gymnasium>=0.29.1
torch>=2.2.0

# Data
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0

# UI
streamlit>=1.35.0

# Live search (optional — mock fallback works without this)
google-search-results>=2.4.2     # SerpAPI client

# Utilities
tqdm>=4.66.0
