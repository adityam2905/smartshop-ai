# Lean dependencies for the tests and the baseline scripts — no torch,
# stable-baselines3 or streamlit, so installs (and CI) stay fast. Install
# requirements.txt as well to train the model or run the app.

numpy>=1.26.0
pandas>=2.2.0
gymnasium>=0.29.1
pytest>=8.0.0
scikit-learn>=1.4.0
